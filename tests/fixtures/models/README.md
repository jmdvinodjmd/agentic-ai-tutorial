# Model qualification fixtures

`qualification/mock_v1.json` is a hand-authored, versioned response sequence
covering structured output, routing, tool choice and arguments, planning,
critique, stopping and recovery after malformed output. It contains no
credentials, private reasoning or provider SDK objects.
