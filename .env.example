# Deterministic default. Other valid values are local and gemini.
MODEL_PROVIDER=mock

# Optional local execution:
# AGENTIC_TUTORIAL_LOCAL_MODEL_PATH=models/local/Qwen3-0.6B-Q8_0.gguf

# Optional Gemini execution. Never commit the real value:
# GEMINI_API_KEY=
