# Retained context

Adds messages retained across turns, demonstrating context rather than a long-term memory database. Retrieval and vector storage are excluded.

```bash
uv run python tutorials/retained_context/run.py
```

Expected retained identifier: `paper-001`.
