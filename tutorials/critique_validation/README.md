# Critique and validation

Adds deterministic validation and one correction of an unsupported identifier. Open-ended judging and unbounded revision are excluded.

```bash
uv run python tutorials/critique_validation/run.py
```

The initial draft fails validation and is revised to cite `paper-001`.
