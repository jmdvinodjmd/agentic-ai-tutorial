# Tool use

Adds a validated read-only catalogue call to the preceding model example. Explicit state and iteration remain excluded.

```bash
uv run python tutorials/tool_use/run.py
```

The deterministic result contains only `paper-001`.
