# Bounded execution and tracing

Adds the shared finite budget and canonical run, budget and termination events. Framework observability is excluded.

```bash
uv run python tutorials/bounded_tracing/run.py
```

Generated events are written beneath `outputs/runs/tutorial-bounded/` and normalised for deterministic comparison.
