# Explicit state

Adds a serialisable `AgentState` containing the task and messages. Planning and memory remain excluded.

```bash
uv run python tutorials/explicit_state/run.py
```

Expected message count: `1`.
